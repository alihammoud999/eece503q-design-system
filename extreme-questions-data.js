// Standalone extremely difficult MCQ set.
// Kept separate from QUESTIONS so these do not mix into the normal bank.

const EXTREME_QUESTIONS = [
  { id:1001, s:"Extreme MCQs", q:"In Lab 6, aws_nat_gateway includes depends_on = [aws_internet_gateway.main], while also referencing aws_eip.nat.id and aws_subnet.public_1.id. Why is depends_on still necessary?", o:["The Internet Gateway must be attached to the VPC before a NAT Gateway can route through it, and no NAT Gateway argument directly references the IGW","Terraform always requires explicit dependencies when more than two resources are involved","depends_on overrides the implicit dependencies and serializes all three resources","Without depends_on, the NAT Gateway would be created in the private subnet"], a:0 },
  { id:1002, s:"Extreme MCQs", q:"A Terraform aws_launch_template uses user_data = base64encode(file(\"userdata/web.sh\")), while aws_instance uses user_data = file(\"userdata/web.sh\"). Why?", o:["Launch templates are encrypted and require base64 for encryption","Launch templates require pre-encoded base64 user_data, while aws_instance handles encoding internally","file() returns binary data for launch templates but plaintext for instances","Base64 allows launch template user_data to exceed the 16 KB limit"], a:1 },
  { id:1003, s:"Extreme MCQs", q:"A backend app takes 25 seconds to initialize. Its livenessProbe starts after 10 seconds and runs every 5 seconds. What happens?", o:["Kubernetes waits 25 seconds, then probes normally","The liveness probe fails during startup and after default consecutive failures Kubernetes restarts the container, causing a crash loop","Kubernetes automatically extends initialDelaySeconds","The pod is removed from Service endpoints but the container keeps running"], a:1 },
  { id:1004, s:"Extreme MCQs", q:"A GitHub Actions build step writes sha=${GITHUB_SHA::8} to GITHUB_OUTPUT. deploy-dev reads needs.build.outputs.image-tag. If the build job outputs block is deleted, what happens?", o:["The workflow fails YAML parsing before jobs run","deploy-dev receives an empty tag and pods enter CrashLoopBackOff","GITHUB_OUTPUT is global across all jobs, so it still works","deploy-dev receives an empty tag and kubectl set image uses a malformed ECR URI, causing ImagePullBackOff"], a:3 },
  { id:1005, s:"Extreme MCQs", q:"Terraform state is in S3 with DynamoDB locking. Developer A's apply hangs after a network timeout. Developer B needs a critical patch. What should B do?", o:["Run terraform apply -lock=false immediately","Wait for DynamoDB's built-in 10 minute lock TTL","Run terraform force-unlock <LOCK_ID> only after confirming A's apply is no longer running","Delete the DynamoDB LockID item directly in the AWS console"], a:2 },
  { id:1006, s:"Extreme MCQs", q:"A Deployment has replicas: 3. Pods request 500m CPU and 256Mi memory, limit 1000m and 512Mi. Two t3.medium nodes have about 1500m CPU available each after overhead. What is the best scheduling answer from the provided exam framing?", o:["6 pods, because limits determine scheduling","4 pods, because memory limits scheduling","3 pods, because the Deployment replica count caps this Deployment","5 pods, because requests allow 3 pods on one node and 2 on the other"], a:3 },
  { id:1007, s:"Extreme MCQs", q:"A Terraform aws_security_group for an ALB has HTTP ingress but the egress block is removed. What is the consequence?", o:["The ALB accepts HTTP but cannot forward to targets or perform health checks because no egress rules exist","The ALB functions normally because security groups are stateful","The ALB forwards requests, but responses to clients are blocked","Terraform validation requires at least one egress block for load balancer security groups"], a:0 },
  { id:1008, s:"Extreme MCQs", q:"In Docker Swarm, API has 3 replicas across a 3-node cluster. Worker-2 has no API container, but a client hits Worker-2 public IP on published port 3000. What happens?", o:["Connection refused because Worker-2 has no API container","The EC2 security group drops the request","Swarm routing mesh accepts the request on Worker-2 and routes it to a healthy API container on another node","Worker-2 forwards only to the manager, which then round-robins"], a:2 },
  { id:1009, s:"Extreme MCQs", q:"Workflow triggers: push branches [main, dev], pull_request branches [main]. A developer pushes feature/login and opens a PR to main. Which runs trigger?", o:["Two runs: push and PR","One run: only pull_request fires","One run: only push fires","No runs"], a:1 },
  { id:1010, s:"Extreme MCQs", q:"Terraform state records aws_instance.example as i-0bc4bbe5b84387543. Someone manually terminates it in AWS. What happens on next terraform plan?", o:["Terraform detects it is gone and proposes creating a new instance","Terraform shows no changes because it trusts state over AWS","Terraform errors and requires terraform state rm","Terraform removes it from state and shows a clean plan"], a:0 },
  { id:1011, s:"Extreme MCQs", q:"In Lab 9 EKS, frontend and backend use ClusterIP services, yet frontend is public. How does external traffic reach it?", o:["The control plane exposes frontend-labeled ClusterIP services automatically","Nginx Ingress Controller pods, exposed through an NLB, route external traffic to the frontend ClusterIP service","EKS ClusterIP services are public by default","eksctl creates an ALB for each ClusterIP service"], a:1 },
  { id:1012, s:"Extreme MCQs", q:"What does Terraform lifecycle prevent_destroy protect on an S3 bucket?", o:["It prevents AWS console or CLI deletion","It makes Terraform error if a Terraform operation would delete the resource, but does not prevent manual AWS deletion","It prevents the bucket from being emptied","It creates a CloudFormation DeletionPolicy"], a:1 },
  { id:1013, s:"Extreme MCQs", q:"Trivy uses exit-code '1' and severity 'CRITICAL,HIGH'. It finds 2 MEDIUM and 0 HIGH/CRITICAL vulnerabilities. What happens?", o:["The scan fails because any vulnerability triggers exit code 1","The scan passes; MEDIUM findings are logged but do not block push","The scan passes but requires reviewer acknowledgment","The scan is skipped because MEDIUM is excluded"], a:1 },
  { id:1014, s:"Extreme MCQs", q:"What does data.aws_availability_zones.available.names[0] resolve to, and why is it preferred over hardcoding?", o:["The first alphabetically sorted AZ; preferred because AZ names map randomly per account","The first AZ returned by the data source; preferred because AZ names differ by region and hardcoding is less portable","The AZ with most available capacity","The default AZ for the account"], a:1 },
  { id:1015, s:"Extreme MCQs", q:"Docker Compose MySQL uses named volume db_data. A developer runs docker compose down then docker compose up -d. What happens to MySQL data?", o:["All data is lost because down destroys named volumes","All data is preserved because named volumes persist across down","Data is preserved only because down was run without -v","Both B and C describe the same behavior"], a:3 },
  { id:1016, s:"Extreme MCQs", q:"During a Kubernetes rolling update from image v1 to v2 with replicas: 3, what is the Deployment, ReplicaSet, and Pod relationship?", o:["Deployment deletes all v1 pods, then creates all v2 pods","Deployment creates a new v2 ReplicaSet, scales it up while scaling the old v1 ReplicaSet down","Deployment directly mutates the existing ReplicaSet template","Deployment creates all v2 pods first, then deletes all v1 pods simultaneously"], a:1 },
  { id:1017, s:"Extreme MCQs", q:"Private EC2 instances use a NAT Gateway in a public subnet for internet downloads. What is the response packet path from the internet back to the instance?", o:["Internet -> IGW -> NAT Gateway -> Private Route Table -> EC2","Internet -> NAT Gateway -> IGW -> Private Route Table -> EC2","Internet -> IGW -> Public Route Table -> NAT Gateway -> Private Route Table -> EC2","Internet -> ALB -> NAT Gateway -> EC2"], a:2 },
  { id:1018, s:"Extreme MCQs", q:"deploy-dev runs only if github.ref is refs/heads/dev; deploy-prod runs only if refs/heads/main. A PR from dev is merged to main. Which deploy runs?", o:["Both deploy-dev and deploy-prod","Only deploy-prod, because the merge creates a push event on main","Only deploy-dev, because dev was the source branch","Neither, because a merge is not a push event"], a:1 },
  { id:1019, s:"Extreme MCQs", q:"Terraform variable key_name has no default. A developer runs terraform apply without providing a value. What happens?", o:["Terraform uses an empty string","Terraform throws validation error during plan","Terraform interactively prompts for key_name","Terraform skips resources that reference it"], a:2 },
  { id:1020, s:"Extreme MCQs", q:"HPA uses averageUtilization: 70 and pods request cpu: 100m. If requests.cpu is removed, what happens?", o:["HPA switches to raw millicore mode","HPA shows <unknown> and cannot calculate percentage utilization","HPA uses node CPU capacity as denominator","HPA falls back to memory scaling"], a:1 },
  { id:1021, s:"Extreme MCQs", q:"Web server SG is changed to allow 0.0.0.0/0 on port 80, but instances are in private subnets with no public IPs. Can external attackers reach them directly?", o:["Yes, because SG allows inbound traffic","No, because there is no public network path to the private instances","Yes, because NAT Gateway forwards inbound traffic","No, because private subnet NACLs always block 0.0.0.0/0"], a:1 },
  { id:1022, s:"Extreme MCQs", q:"Terraform DynamoDB locking table is created with primary key lockid instead of LockID. What happens?", o:["Terraform treats the key as case-insensitive","Terraform writes LockID anyway","Terraform fails to acquire the lock because the key name and capitalization do not match","Terraform creates a LockID attribute automatically"], a:2 },
  { id:1023, s:"Extreme MCQs", q:"In Docker Swarm, a MySQL service with a local db_data volume is constrained to node.role == manager. Why is this critical?", o:["Manager nodes have enhanced file security","Docker volumes are local; rescheduling DB to another node loses access to the original node's data volume","Manager nodes have special Swarm storage","The constraint prevents DB replication across nodes"], a:1 },
  { id:1024, s:"Extreme MCQs", q:"Terraform workflow has paths: terraform/**. A commit modifies app/server.js and terraform/main.tf in a PR to main. Which workflows trigger?", o:["Only CI triggers because Terraform excludes mixed commits","Only Terraform triggers because path workflows take precedence","Both CI and Terraform trigger because each workflow evaluates independently","Neither triggers because mixed changes fail path filters"], a:2 },
  { id:1025, s:"Extreme MCQs", q:"A pod readiness probe fails but liveness passes. What is the pod state?", o:["The pod is killed and restarted","The container keeps running, but the pod is removed from Service endpoints and receives no new Service traffic","The pod is evicted and rescheduled","ReplicaSet creates a replacement pod"], a:1 },
  { id:1026, s:"Extreme MCQs", q:"A subnet references aws_vpc.main.id, creating an implicit dependency. If depends_on = [aws_vpc.main] is added too, what is the effect?", o:["Terraform throws circular dependency","It is redundant but harmless","It changes an advisory order into a strict order","It waits for all VPC dependents before creating the subnet"], a:1 },
  { id:1027, s:"Extreme MCQs", q:"Dockerfile: COPY . . before RUN npm install --production. Developers frequently change source but rarely dependencies. What is the performance problem?", o:["EXPOSE before CMD slows the build","Every source change invalidates the npm install cache layer, forcing dependency reinstall","WORKDIR creates expensive image overhead","--production prevents dependency caching"], a:1 },
  { id:1028, s:"Extreme MCQs", q:"Images are tagged with the first 8 chars of GITHUB_SHA. Deploy uses kubectl set image instead of kubectl apply -f. Why?", o:["set image avoids manifest validation","set image updates the Deployment image without hardcoding and recommitting the new tag into YAML manifests","apply recreates all pods simultaneously while set image rolls by default","set image bypasses admission controllers"], a:1 },
  { id:1029, s:"Extreme MCQs", q:"Private subnet route table has 0.0.0.0/0 to NAT. Private EC2 sends to public subnet EC2 at 10.0.1.50 in same 10.0.0.0/16 VPC. Does traffic use NAT?", o:["Yes, all private subnet traffic uses NAT","No, the local 10.0.0.0/16 route is more specific than 0.0.0.0/0","Yes, private subnet has no public subnet route","No, because VPC peering is automatically enabled"], a:1 },
  { id:1030, s:"Extreme MCQs", q:"Docker Compose API depends_on db with condition: service_healthy. DB healthcheck never passes. What happens?", o:["API starts after a default 60-second timeout","API is never started; Compose keeps waiting for DB to become healthy","API starts and retries five times","API starts but is marked unhealthy"], a:1 },
  { id:1031, s:"Extreme MCQs", q:"Terraform backend key is prod/terraform.tfstate. Team wants staging with same code but different state. Correct approach?", o:["Change key to staging/terraform.tfstate and run terraform init -reconfigure","Use a second S3 bucket for staging","Use terraform workspace new staging","Both A and C are valid state isolation approaches"], a:3 },
  { id:1032, s:"Extreme MCQs", q:"Ingress routes / to frontend-service. Frontend Nginx proxies /api/visits to backend. Backend readiness fails for all pods. What does user see?", o:["502 Bad Gateway because backend Service has no ready endpoints","404 because Ingress has no /api/visits rule","Request hangs indefinitely while Kubernetes retries","Cached response from Ingress Controller"], a:0 },
  { id:1033, s:"Extreme MCQs", q:"Terraform plan shows 0 to add, 1 to change, 1 to destroy. The destroyed resource is an aws_security_group. What is happening?", o:["Terraform is replacing it due to immutable change","Terraform is destroying an orphaned SG while changing another resource","Terraform is destroying drift to recreate from code","Both A and B are plausible depending on code changes or removal"], a:3 },
  { id:1034, s:"Extreme MCQs", q:"Actions jobs: test takes 45s, build needs test and fails at Trivy after checkout/login/tag/docker builds, deploy needs build. How long before vulnerability feedback?", o:["45 seconds","About 45 seconds plus time to reach Trivy inside build","About full test plus full 3-minute build","45 seconds because Trivy runs in parallel"], a:1 },
  { id:1035, s:"Extreme MCQs", q:"kubectl delete pod backend-abc123 where pod is managed by Deployment replicas: 2. What happens?", o:["Pod is permanently deleted, leaving 1 replica","Deployment controller detects mismatch and creates a pod","ReplicaSet controller creates the replacement, Deployment is not involved in that direct action","Both B and C accurately describe the controller chain"], a:3 },
  { id:1036, s:"Extreme MCQs", q:"Terraform variable has sensitive = true. An output references it but output is not marked sensitive. What happens?", o:["Terraform silently masks it","Terraform errors and refuses to expose sensitive value through non-sensitive output","Terraform displays it but redacts state","Terraform warns but proceeds"], a:1 },
  { id:1037, s:"Extreme MCQs", q:"Docker Swarm update_config has parallelism: 1 and delay: 10s. With 5 API replicas, what is minimum rolling update time?", o:["50 seconds exactly","40 seconds exactly","10 seconds","Approximately 50 seconds plus startup and health-check time for replacements"], a:3 },
  { id:1038, s:"Extreme MCQs", q:"A pod gets MAX_ITEMS from ConfigMap using envFrom. ConfigMap changes from 100 to 200. When does pod see new value?", o:["Immediately","After kubelet sync period","Only after pod restart, because env vars are set at pod creation","After rollout restart without deleting pods"], a:2 },
  { id:1039, s:"Extreme MCQs", q:"ALB rules: /api/* -> api-tg and /* -> web-tg. Request path /api/health. Which target group receives it?", o:["web-tg because broader rules win","api-tg because ALB evaluates rule priority and specific /api/* is normally higher priority","Both target groups","web-tg because /* is the default action"], a:1 },
  { id:1040, s:"Extreme MCQs", q:"Terraform workflow runs terraform plan -no-color -out=tfplan. Why use -no-color?", o:["It reduces artifact size","It removes ANSI escape codes so PR comments render readable plain text","It reduces CPU usage","It forces JSON output"], a:1 },
  { id:1041, s:"Extreme MCQs", q:"EKS has two t3.medium worker nodes and Nginx Ingress creates an NLB. One node fails. What happens?", o:["NLB health checks stop traffic to failed node, and Kubernetes reschedules pods on surviving capacity","NLB keeps sending 50% traffic to failed node","EKS immediately provisions a replacement and migrates pods before NLB detects failure","Ingress pods take over failed node IP via ARP"], a:0 },
  { id:1042, s:"Extreme MCQs", q:"Terraform data aws_ami uses most_recent = true. Amazon publishes a newer matching AMI after last apply. What happens on next plan?", o:["No change because AMI ID is cached","Data source re-queries AWS, and resources using the AMI may be replaced with the newer AMI","Data source results are never re-evaluated","Terraform only informs unless ignore_changes is set"], a:1 },
  { id:1043, s:"Extreme MCQs", q:"GitHub Actions environment prod has required reviewer. A developer pushes directly to main. What happens to deploy-prod?", o:["Runs immediately because direct pushes bypass environment protection","Job waits for reviewer approval in GitHub UI","Job is skipped because environment rules only apply to PR workflows","Job fails because only PRs can deploy protected environments"], a:1 },
  { id:1044, s:"Extreme MCQs", q:"Terraform creates VPC, subnets, IGW, and NAT Gateway. During terraform destroy, what order is used?", o:["Creation order","Reverse dependency order, deleting dependents first","Alphabetical by resource type","All in parallel with AWS handling conflicts"], a:1 },
  { id:1045, s:"Extreme MCQs", q:"Dockerfile has EXPOSE 3000. Developer runs docker run -d my-app:v1 without -p. Can external clients reach port 3000?", o:["Yes, EXPOSE opens the host port","No, EXPOSE is metadata and no host port is mapped","Yes, but only from containers on same bridge network","Both B and C are correct"], a:3 },
  { id:1046, s:"Extreme MCQs", q:"Kubernetes Secret stores API_TOKEN: TXlTM2N1cjNUb2tlbg==. Is this encrypted?", o:["Yes, Secrets are AES-256 encrypted by default","No, it is base64 encoding, not encryption, and anyone with Secret access can decode it","Yes, type Opaque means encrypted with cluster master key","No, but API server to etcd transit encryption is enough"], a:1 },
  { id:1047, s:"Extreme MCQs", q:"Pipeline uses npm ci. Developer updates package.json but forgets to update package-lock.json. What happens?", o:["npm ci installs from package.json and succeeds","npm ci fails because package.json and lockfile are out of sync","npm ci ignores package.json and app crashes later","npm ci updates lockfile automatically and continues"], a:1 },
  { id:1048, s:"Extreme MCQs", q:"Terraform apply job has if: github.ref == refs/heads/main && github.event_name == push. Workflow triggered by pull_request to main. What happens?", o:["Plan and apply both run","Only plan runs; apply is skipped because event_name is pull_request","Neither job runs","Apply runs but waits for prod approval"], a:1 },
  { id:1049, s:"Extreme MCQs", q:"VPC-1 peers with VPC-2, and VPC-2 peers with VPC-3. Can VPC-1 instances communicate with VPC-3 through VPC-2?", o:["Yes, VPC-2 acts as a transit hub","No, VPC peering is non-transitive; direct peering or Transit Gateway is required","Yes, if route tables are configured","No, each VPC can only have one peering connection"], a:1 },
  { id:1050, s:"Extreme MCQs", q:"Branch protection on main requires only the Lint and Test check. PR from dev to main has test pass, build pass until Trivy finds CRITICAL and build fails. Can developer merge?", o:["No, any build failure blocks merging","Yes, only the selected required check blocks merging; build failure does not block if not required","No, branch protection blocks any failed job","Yes, but only admins can bypass"], a:1 },
];

window.EXTREME_QUESTIONS = EXTREME_QUESTIONS;
